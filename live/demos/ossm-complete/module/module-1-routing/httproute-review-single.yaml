apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: reviews-route
  namespace: bookinfo
spec:
  parentRefs:
  - group: ""
    kind: Service
    name: reviews
    namespace: bookinfo
  rules:
  - backendRefs:
    - name: reviews-v1
      port: 9080
      weight: 100